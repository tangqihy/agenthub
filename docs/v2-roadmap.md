# V2 Roadmap (Deferred)

## 微信登录 + CloudBase

- `@cloudbase/js-sdk` + `@cloudbase/adapter-wx_mp`
- 读路径切换 CloudBase SDK + `watch()`
- 控制 API 保留 FastAPI（火山云 HTTPS）

## 家庭共享

- 角色：`Owner` / `Member`（V1 不做 Admin/Guest）
- CloudBase Security Rules 按 `auth.openid` 隔离

## 实施前置

- 微信小程序 AppID
- CloudBase 环境 ID
- HTTPS 合法域名
